## Server
server:
  port: ${SERVICE_UMS_PORT}

## MySQL
spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    # url: jdbc:mysql://10.10.200.49:3306/ums?serverTimezone=UTC&useLegacyDatetimeCode=false
    url: ${URL_DB_UMS}
    username: ${USERNAME_DB}
    password: ${PASSWORD_DB}

